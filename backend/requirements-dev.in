# =========================================================================
# Human-edited dev dependency intent file. requirements-dev.txt is the
# generated lock — never edit it by hand. Regenerate (after the prod lock,
# which constrains it to the same versions) with:
#
#   uv pip compile requirements-dev.in -o requirements-dev.txt --universal --python-version 3.11 --generate-hashes -c requirements.txt
# =========================================================================

-r requirements.in

pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=6.0
aiosqlite>=0.20
# [lua] pulls lupa: the limiter's sliding window is a Lua script, so a Lua-less
# fake would not exercise the code that actually runs in production.
fakeredis[lua]>=2.37.0
ruff>=0.16.3
