# --- Secrets & env ---
# Allowlist: everything .env* is ignored except the committed templates.
.env*
!.env*.example
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
id_rsa*
id_ed25519*
secrets/
config.local.*
*.p8
*.keystore
*.jks
service-account*.json
*credentials*.json
docker-compose.override.yml

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage*
coverage.xml
htmlcov/

# --- Node / Next.js ---
node_modules/
.next/
out/
dist/
build/
coverage/
*.tsbuildinfo
next-env.d.ts
.eslintcache
.turbo/
.swc/
.vercel
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# --- Logs & local databases ---
*.log
logs/
*.db
*.sqlite
*.sqlite3

# --- Editors / OS ---
.vscode/
.idea/
*.swp
*~
.DS_Store
._*
Thumbs.db
desktop.ini

# --- Local agent state ---
.claude/
# Private engineering log: build history, open defects, roadmap. CLAUDE.md is public.
CLAUDE.local.md
# Transient quality-sweep working notes: verbatim prompts and model output.
OVERNIGHT-QUALITY-REPORT.md

# --- Docker volumes (local data) ---
.data/

# --- Local backup-script test output (scripts/backup.sh) ---
.backups/


# Superpowers SDD scratch (plans/specs live under docs/, this is transient)
.superpowers/

# --- Documentation site (MkDocs) ---
# Build output.
/site/
# CONTRIBUTING.md and SECURITY.md live in the repository root because GitHub
# looks for them there; the docs build copies them into docs_dir. The copies
# are generated, so only the root originals are tracked.
/docs/CONTRIBUTING.md
/docs/SECURITY.md
